import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#313131',
    },
    secondary: {
      main: '#8c8b8b',
      },
    }
});

export default theme;
